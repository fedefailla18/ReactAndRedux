import React from "react";

const AboutPage = () => (
  <div>
    <h2>About </h2>
    <p> React, Redux, Router and many others are used to make this project </p>
  </div>
);

export default AboutPage;
